# backend

ここはbackendの処理を書くフォルダです

## 起動

`backend` フォルダ内で実行:

```sh
make dev
```

ログを見る:

```sh
make logs
```

API:

- `http://localhost:8000/`
- `http://localhost:8000/health`
- `http://localhost:8000/docs`

停止:

```sh
make down
```
